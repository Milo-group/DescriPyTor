# Web app
streamlit>=1.30

# Core toolkit
pandas
pyarrow            # read .feather

# Optional descriptor-engine deps (install the ones you want to expose online).
# Engines whose deps are missing simply skip themselves with a message.
# rdkit
# ase
# mordred
# deepchem
# morfeus-ml
# moltop
# networkx
# autoqchem        # qm engine (Gaussian logs)
# aqme             # aqme_qdescp / csearch  (also needs a working xTB binary on PATH)
# torch            # only needed for data_extractor.py's get_renumbering_dict()
#                  # (MolAlign.renumbering) - not used by feather loading, tab 1,
#                  # or any of the descriptor engines. Lazily imported, so it's
#                  # only required if you actually call that method.
# dash             # only needed for utils.visualize's dipole plotting helpers -
#                  # also lazily imported and unused by the rest of the webapp.

# utils/help_functions.py (imported eagerly - core dependency, NOT optional)
# needs plotly + ipywidgets at module level:
# plotly
# ipywidgets

# Modeling tab (page 4)
# scikit-learn statsmodels    # M3 linear/lasso feature-combination search
# bassa-reg                   # BASSA Bayesian regression engine

# Results tab: PCA/UMAP scatter of the merged feature set
# scikit-learn      # PCA + StandardScaler/SimpleImputer (also needed by M3 above)
# umap-learn        # UMAP option; PCA still works without it
# plotly            # interactive scatter (st.plotly_chart)

# NOTE: the DescriPytor package itself is NOT on PyPI - it must be present on the
# server at the "MolFeatures root" you set in the app sidebar (the same folder the
# notebooks use).
