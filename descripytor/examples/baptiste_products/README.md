Baptiste product set: 18 `.feather` files plus experimental `output` values
in `outcomes.csv` (column `name` matches the feather stem).

Reference structure: `unsub.feather`. Atom presets in `presets.json` match
the Baptiste Products notebook (1-based indices).
